# Corpus
Synthetic company handbook is GENERATED here at milestone M2 (see commands/phases.md).
Must include: leave policy, public holidays, product specs, FAQ; at least one MULTI-HOP
fact (leave policy + public holidays) and content some questions genuinely can't answer
(for the refusal test). To use real docs instead, drop them here and rewrite eval/golden.jsonl.
